<Window x:Class="KeeperPRO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeeperPRO"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="1050" MinHeight="450" MinWidth="1050">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="393*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="ХранительПРО" Style="{StaticResource HeadingText}"/>
        <!--<StackPanel Orientation="Vertical" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,116,0,0">
            <TextBlock Text="Авторизация" Style="{StaticResource HeadingText}" />
            <TextBlock Text="Введите код сотрудника" Margin="5" HorizontalAlignment="Center"/>
            <TextBox x:Name="tbCodeEmployee"/>
            <Button Content="Авторизоваться" Margin="5" Click="Button_Click" IsDefault="True"/>
        </StackPanel>-->
        <Frame x:Name="Frame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
